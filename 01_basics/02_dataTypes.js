"use strict"; // treat all JS code as a newer version

// alert(3 + 3); // We are using Node.js, not the browser

console.log(3
     +
      3) // Code readability should be high

console.log("Chelsi");

let name = "Chelsi";
let age = 18;
let isLoggedIn = false;
let state; // This could be initialized if necessary

// Types:
// number => represents numbers (e.g., 2 to the power of 53)
// bigint => represents integers with arbitrary precision
// string => represents text (e.g., "")
// boolean => represents true or false
// null => represents a standard value indicating no value
// undefined => represents a variable that has been declared but not assigned a value
// symbol => unique identifier
// object => represents a collection of key-value pairs

console.log(typeof undefined);
